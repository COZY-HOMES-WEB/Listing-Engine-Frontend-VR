(function($){"use strict";function formatFullDate(dateStr){if(!dateStr)return"-";const date=new Date(dateStr);if(isNaN(date))return dateStr;return date.toLocaleDateString("en-US",{month:"long",day:"numeric",year:"numeric"})+" "+date.toLocaleTimeString("en-US",{hour:"2-digit",minute:"2-digit"});}function formatSimpleDate(dateStr){if(!dateStr)return"-";const date=new Date(dateStr);if(isNaN(date))return dateStr;return date.toLocaleDateString("en-US",{month:"long",day:"numeric",year:"numeric"});}function capitalize(str){if(!str)return"";return str.charAt(0).toUpperCase()+str.slice(1);}function show(data){if(!data)return;const $panel=$("#lef-my-book-panel");const $container=$("#lef-mb-view-detail-card");if($container.length===0){console.error("LEF View: #lef-mb-view-detail-card not found in DOM. Check view.php is included.");return;}$container.find("#lef-mb-view-prop-title").text(data.property_title||"N/A");$container.find("#lef-mb-view-updated-at").text(formatFullDate(data.updated_at));$container.find("#lef-mb-view-property-link").attr("href",data.property_url||"#");$container.find("#lef-mb-view-status").text(capitalize(data.status)).attr("data-status",data.status);$container.find("#lef-mb-view-res-number").text(data.reservation_number||"-");$container.find("#lef-mb-view-total-price").text("₹"+parseFloat(data.total_price||0).toLocaleString("en-IN",{minimumFractionDigits:2}));$container.find("#lef-mb-view-created-at").text(formatFullDate(data.created_at));try{const dates=typeof data.reserve_date==="string"?JSON.parse(data.reserve_date):data.reserve_date;$container.find("#lef-mb-view-check-in").text(formatSimpleDate(dates.check_in));$container.find("#lef-mb-view-check-out").text(formatSimpleDate(dates.check_out));}catch(e){$container.find("#lef-mb-view-check-in").text("-");$container.find("#lef-mb-view-check-out").text("-");}try{const guests=typeof data.total_guests==="string"?JSON.parse(data.total_guests):data.total_guests;const parts=[];if(guests.adults>0)parts.push(guests.adults+" adults");if(guests.children>0)parts.push(guests.children +" children");if(guests.infants>0)parts.push(guests.infants+" infants");$container.find("#lef-mb-view-total-guests").text(parts.join(", ")||"1 Guest");}catch(e){$container.find("#lef-mb-view-total-guests").text("1 Guest");}$panel.find("> header, > nav, > section").hide();$container.show();$("html, body").animate({scrollTop:$panel.offset().top -80},400);}function hide(){const $panel=$("#lef-my-book-panel");const $container=$("#lef-mb-view-detail-card");$container.hide();$panel.find("> header, > nav, > section").fadeIn(250);}$(document).on("click","#lef-mb-view-back-btn",function(){hide();});window.LefMbView={show:show,hide:hide};})(jQuery);